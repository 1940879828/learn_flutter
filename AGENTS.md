# learn-flutter Agent Guide

## 交流与定位

- 和用户交流使用简体中文。
- 本仓库是 Flutter/Dart 学习项目，不是生产业务仓库。
- 学习对象仓库：`/Users/dev/Documents/Projects/flutter-spell-ai`。
- 用户背景：Web 前端开发，熟悉 Nuxt、Next、TypeScript。
- 学习主线：先系统学习 Flutter/Dart，再逐步接手 `flutter-spell-ai`。

## 每次 Session 启动时先读

1. `notes/00-前言.md`：理解学习目标、边界和方法。
2. `notes/01-索引.md`：确认当前章节、学习顺序和产出标准。
3. `notes/99-学习日志与任务看板.md`：查看最近进度、阻塞点和下一步。
4. 如本次问题涉及目标项目，再读取 `/Users/dev/Documents/Projects/flutter-spell-ai/AGENTS.md` 和相关源码。

## 学习文档维护规则

- 所有学习笔记放在 `notes/`。
- 章节命名使用两位数字前缀，例如 `02-Dart基础与TS迁移.md`。
- 每章尽量包含：学习目标、Web 前端迁移映射、核心概念、练习任务、目标项目观察、完成标准。
- 新增学习章节、方法论手册或项目导读时，优先套用 `notes/98-笔记模板.md` 的结构。
- 不要把学习笔记写成泛泛教程；每章都要说明它未来如何帮助接手 `flutter-spell-ai`。
- 学习过程中发现目标项目关键模块时，补充到 `notes/12-flutter-spell-ai接手导读.md`。
- 每次 session 结束前，更新 `notes/99-学习日志与任务看板.md` 的最近进度和下一步。

## Flutter 命令约定

- 本项目和目标项目都优先使用 FVM。
- 常用命令：
  - `fvm flutter --version`
  - `fvm flutter pub get`
  - `fvm flutter analyze`
  - `fvm flutter test`
  - `fvm flutter run`

## 目标项目已知线索

- Flutter 版本：目标项目 `flutter-spell-ai` 声明 Flutter `3.35.7`，Dart SDK `>=3.9.0 <4.0.0`。
- 核心方向：AI 图片/视频生成、图库、路由、状态管理、网络、Firebase、内购、多语言、媒体保存与预览。
- 重点依赖：`flutter_riverpod`、`hooks_riverpod`、`go_router`、`dio`、`firebase_*`、`sqflite`、`image_picker`、`photo_manager`、`video_player`、`permission_handler`。

## 工作方式

- 先学习概念，再做最小练习，再回看目标项目代码。
- 对用户解释时，优先用 Nuxt/Next/TypeScript 的类比建立迁移路径。
- 解释代码时，使用用户已经懂的 Web/TS 类比，不要先讲 API 定义。
- 先用一句大白话说明“这行代码为什么存在”，再解释类型要求。
- 如果涉及 fallback、null、泛型、监听器，先说业务直觉，再补代码。
- 每次最多给一个小代码片段，避免一次塞太多 API 细节。
- 对不确定的 Flutter API、插件行为、版本差异，先查本地代码或官方资料，不凭记忆硬答。
- 未经用户明确授权，不 push、不创建 PR、不改动目标项目生产代码。

## 开发约定

- 本仓库使用 Flutter/Dart；`.dart` 代码遵守 `analysis_options.yaml` 和 Dart analyzer 结果。
- 修改 Flutter 代码前，先阅读相关 Widget、Provider、路由、测试和调用方，避免只看单个文件就下结论。
- 练习代码优先放在本项目 `lib/` 和 `test/` 中；目标项目代码只读，除非用户明确要求修改。
- UI 练习优先保持简单、可运行、可解释；不要为了学习 demo 过早引入复杂架构。
- Widget 拆分以状态边界、复用价值和可读性为准，不为了“拆小”而机械拆分。
- 异步、权限、文件、音视频、相册、生命周期相关代码必须显式考虑 loading、error、dispose、权限拒绝和平台差异。
- 代码生成文件通常不要手改，例如 `*.g.dart`、本地化生成文件；需要更新时使用项目约定的生成命令。
- 资源文件需要在 `pubspec.yaml` 中声明后再使用，并确认路径、缩进和平台大小写差异。
- 不要覆盖或回退用户已有的未提交修改；如修改与现有工作重叠，先保留并基于当前内容编辑。
- API key、用户配置、会话记录和本地 mock 数据不得写入源码、日志或提交内容。

## 提交代码时

- 提交信息使用 `类型: 中文描述` 的简式风格。
- 常用类型：`feat`、`fix`、`docs`、`test`、`refactor`、`chore`、`update`、`delete`。
- 未经用户明确授权，不 push、不创建 PR。

## 代码风格守则

- 代码行为符合直觉，副作用受控；能写成纯函数的业务逻辑优先写成纯函数。
- 命名要表达业务含义，避免 `data`、`tmp`、`info`、`a`、`b` 这类无信息量命名。
- 优先 early return，减少深层嵌套；复杂条件要拆成能读懂的中间变量或小函数。
- 组合优于继承；Flutter 中优先用 Widget 组合表达 UI，而不是制造复杂继承层级。
- 状态尽量靠近使用处；确实跨页面、跨模块共享时，再提升到 Riverpod/provider/service 层。
- 不过早抽象；当第二处或第三处重复暴露出稳定模式时，再提取公共 Widget、helper 或 service。
- 注释只解释“为什么”和关键约束，不复述代码“做了什么”。
- 修改代码逻辑时，同步更新相关学习笔记、TODO、注释或测试，避免文档和代码漂移。
- Dart collection API、extension、record、pattern matching 可以使用，但以可读性为先，不为炫技而写。
- 第一性原理：回归问题本质思考，不盲从现有方案，不复制惯性做法。
- 奥卡姆剃刀：如无必要勿增实体，优先选最简单可验证的实现。

## 第一性原理

遇到需求冲突、方案分歧、或要分析复杂需求时，回到第一性原理：

- 抛开“惯例就这么做”“上次那样做的”，先问这件事的根本目标和真实约束是什么。
- 把问题拆到不可再拆的事实层，再从事实重新推导方案，而不是类比套用现成答案。
- 冲突时先对齐双方真正要的底层目标——冲突往往只在表层，底层目标常可调和。

## 禁止症状遮蔽式工程

从第一性原理出发。遇到加载慢、白屏、闪烁、状态错乱、异步竞态、生命周期错位、偶现失败、媒体播放异常、权限异常等问题时，禁止把延迟、假 loading、静默吞错、无限重试、强制刷新、缓存旧画面、提前 dispose 规避报错等手段当作根因修复。

判断标准：

- 必须先解释真实因果链：哪个状态未就绪、哪个依赖缺失、哪个边界没有建模、哪个链路变慢或失败。
- 修复方案必须优先消除根因，而不是只降低用户感知或让异常变得不可见。
- Flutter 中尤其要检查：`BuildContext` 是否已失效、controller 是否正确初始化/释放、provider 是否被错误刷新、Future 是否存在竞态、权限状态是否完整建模、平台配置是否缺失。
- 如果引入 `Future.delayed`、`addPostFrameCallback`、额外 loading、吞错、重试、强刷、缓存旧状态等机制，提交前必须回答：这是根因修复还是症状遮蔽？
- 临时止血可以接受，但必须显式标注为 temporary mitigation，并写明退出条件、验证方式和后续根因修复任务；不得把止血包装成最终方案。

## 代码审查守则

- 审查以验证行为是否符合需求、注释、类型契约和测试为目标；先还原预期行为，再判断实现是否矛盾，不以“找出问题”为前提。
- 正确性结论必须给出完整证据链：具体输入或状态、实际经过的分支、产生的结果、与预期的差异；三元表达式和复杂条件须分别代入真假分支验证。
- 结构相似或成对出现的代码必须独立追踪执行语义，再比较差异；不得依据命名、排列或表面对称性推断另一侧行为。
- 判断局部代码前须核对相关调用方、状态转换和已有测试；可运行时用最小复现或测试验证，不根据孤立代码片段下结论。
- 明确区分“已确认缺陷”“潜在风险”和“产品取舍”；只有可复现且违反既定契约的行为才标记为缺陷，不确定项须说明假设与验证缺口。

## 异步工具库使用提示

本项目已直接依赖 `package:async`。当实现或讲解 Dart/Flutter 异步代码时，除了 Dart 内置的 `Future`、`Stream`、`async/await`，还应主动考虑 `package:async` 是否能让代码更清晰。

优先联想到以下工具：

- `CancelableOperation`：页面退出、用户取消、重复点击、搜索请求废弃等需要取消异步结果的场景。
- `AsyncMemoizer`：初始化、配置加载、资源预热等“只允许执行一次”的异步任务。
- `AsyncCache`：远程配置、轻量接口、本地资源读取等需要短期缓存异步结果的场景。
- `StreamQueue`：测试或业务中需要按顺序消费 `Stream` 事件的场景。
- `StreamGroup`：需要合并多个 `Stream` 来源的场景。
- `FutureGroup`：需要收集、统一等待一组动态添加的 `Future` 的场景。
- `RestartableTimer`：防抖、倒计时重置、用户连续输入后延迟触发的场景。

不要为了使用库而使用库。简单的 `await`、`Future.wait`、普通 `Stream.listen` 能清楚表达时，优先保持简单。

## 组件组织与拆分规则

后续 Agent 实现 UI 需求时，应优先延续本项目已有的 feature-first 组织方式，避免为了单个需求引入新的目录体系或抽象风格。

### 放置位置

* 新页面、新业务组件优先放在 `lib/ui/pages/<feature>/` 对应业务目录下。
* 仅当组件满足“跨多个业务域复用、语义稳定、不依赖具体业务状态”时，才放入 `lib/ui/widget/`。
* 页面内临时复用、只服务当前 feature 的控件，应放在该 feature 的 `widget/` 或 `widgets/` 子目录，不要提前提升为全局组件。
* 状态管理、数据装配、业务动作应优先放在 feature 内的 `provider/`、`providers/`、`handler/`、`mixin/` 或同目录 `.vm.dart` 文件中，避免散落到全局工具层。

### 页面职责

* 页面入口文件应主要负责：路由参数、页面生命周期、顶层 Scaffold/导航栏、页面级模式切换、少量顶层编排。
* 页面主体 UI 复杂时，应下沉到独立 Widget，例如放入 feature 内 `widget/` 目录。
* 页面文件不应长期同时承载大量布局、网络请求、订阅监听、打点、弹窗、权限判断、上传/下载、错误处理。
* 修改已有页面时，优先沿用该页面附近已有的 `.ui.dart`、`.vm.dart`、`provider/`、`widget/` 拆分方式，保持局部一致。

### UI、状态与副作用

* Widget 构建函数应以展示和交互编排为主，不要直接堆叠复杂网络调用、持久化、打点、全局事件订阅或跨页面导航决策。
* 网络请求、任务创建、上传下载、权限流程、错误恢复、打点等副作用，应抽到 provider、handler、VM/mixin 或明确命名的方法中。
* UI 组件优先通过构造参数、callback、`ValueListenable`、Riverpod provider 等方式接收状态和动作，不要隐式读取不相关的全局状态。
* 可复用 Widget 应尽量保持输入输出明确，避免直接依赖页面 State 的私有字段；确实需要访问页面私有状态时，才使用 `part` 或私有类。

### `part`、`.ui.dart` 与 `.vm.dart`

* `part` 适合拆分同一个页面的私有实现，但它不是解耦手段；使用时必须保持职责清晰。
* `.vm.dart` 优先放页面状态、订阅、数据加载、事件处理、导航和副作用流程。
* `.ui.dart` 优先放构建 UI 的扩展方法或私有 Widget，不要混入复杂业务副作用。
* 不要让 `.ui.dart`、`.vm.dart` 之间通过大量隐式私有字段形成难以追踪的耦合；字段过多时应改为显式参数、独立 Widget 或 provider。

### 拆分触发条件

出现以下任一情况，应优先考虑拆分，而不是继续在同一文件追加代码：

* 一个 Widget 同时处理三类以上职责，例如布局、请求、订阅、打点、弹窗、权限、文件处理、错误恢复。
* build 方法或单个构建函数需要频繁滚动才能理解，且包含多个可命名的 UI 区块。
* 同一组 UI 和交互在同一 feature 内出现两次以上。
* 某段逻辑需要独立测试、复用或单独处理生命周期。
* 新需求需要修改全局组件才能完成，但该行为只属于当前业务域。

### 抽象边界

* 不要因为样式相似就立即抽全局组件；只有语义一致、行为一致、未来变化方向也一致时才抽象。
* 抽出来的组件名称应表达业务语义或稳定 UI 语义，避免 `CommonItem`、`BaseWidget`、`CustomView` 这类无法指导使用场景的命名。
* 全局组件不得反向依赖具体 page、provider、router 参数或业务模型；如必须依赖，说明它应该留在 feature 目录内。
* 修改 `lib/ui/widget/`、`lib/common.dart`、`lib/app.dart` 等共享层前，应先确认没有更小范围的 feature 内实现方式。

### Agent 实现约束

* 开始实现前，先阅读目标 feature 附近 2-3 个相关文件，确认本地命名、状态管理和拆分习惯。
* 优先小范围修改现有结构；不要为一个普通需求重排目录、批量重命名或引入新的架构模式。
* 新增文件时，应能用一句话说明其职责；如果职责需要用“以及/同时/顺便”描述，通常应继续拆分。
* 代码完成后，应自查是否避免了五类失败：目录放错、页面继续膨胀、UI/状态/副作用混杂、过度抽全局组件、改动范围失控。
