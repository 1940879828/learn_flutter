# 07 异步、网络与数据模型

## 学习目标

- 掌握 Flutter/Dart 中异步请求、错误处理和取消/防抖思路。
- 理解 Dio、Repository、Model、JSON 生成的协作。
- 能读懂目标项目的远程接口和数据模型。

## Web 前端迁移映射

- `fetch/axios` 对应目标项目中的 `dio`。
- React Query/SWR 的 loading/error/data 状态，在 Flutter 中常由 `FutureBuilder`、Riverpod 或自定义状态承载。
- TypeScript DTO/interface 在 Dart 中常通过 class + `json_serializable` 实现。
- Web 拦截器、token、错误码处理，对应 Dio interceptors 和统一异常封装。

## 核心概念清单

- `Future`
- `Stream`
- `try/catch/finally`
- Dio client、interceptor、cache
- repository 分层
- model 序列化
- `build_runner`
- 错误模型和业务异常

## 最小练习

做一个模拟 API：

- 定义 `ApiClient`。
- 模拟请求任务列表和任务详情。
- 定义 model 的 `fromJson/toJson`。
- 页面展示 loading/error/success。

## 目标项目观察

优先阅读：

- `/Users/dev/Documents/Projects/flutter-spell-ai/lib/repository/remote/`
- `/Users/dev/Documents/Projects/flutter-spell-ai/lib/engine/net/`
- `/Users/dev/Documents/Projects/flutter-spell-ai/lib/exception/`
- `/Users/dev/Documents/Projects/flutter-spell-ai/lib/model/result.dart`

观察问题：

- 目标项目的 API 统一入口在哪里？
- 错误如何从网络层传到 UI？
- 哪些 model 是业务核心？

## 完成标准

- 能解释 Dio interceptor 的作用。
- 能跑通一次本地模拟异步请求。
- 能读懂一个目标项目 API 方法和对应 model。

## QA 问答（面试官常问）

### Q1：`Future` 和 `Stream` 的边界是什么？
`Future` 表示一次性结果，`Stream` 表示持续事件流。前者一次成功/失败，后者可持续更新。面试里常考 async 数据模型如何选择。

### Q2：Dio 的拦截器一般做哪些事？
常见是注入 token、统一错误码处理、统一日志、重试策略、超时与缓存策略。它对应 Web 里 interceptor/middleware 的职责。

### Q3：`Repository` 层为什么重要？
它把页面和网络细节解耦，形成“调用协议稳定、实现可替换”的边界。面试常问这个层能带来哪些可测性和维护收益。

### Q4：出现接口报错时，UI 与业务层怎么协作？
网络层产出标准化错误模型，业务层决定是否重试/降级，UI 决定展示内容与文案。不要把所有错误都抛给 UI 做业务判断。

### Q5：生成 JSON 模型为什么要自动化？
减少手写转换错误，保证类型与字段一致，并支持重构时更快发现不匹配，尤其在模型字段频繁变化的项目里很关键。
