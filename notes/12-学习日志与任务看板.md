# 12 学习日志与任务看板

## 当前学习路线

系统学习 Flutter/Dart，然后再接手 `/Users/dev/Documents/Projects/flutter-spell-ai`。

## 当前阶段

- 阶段：进入 Flutter 状态管理与 Riverpod 练习。
- 当前章节：`05-状态管理与Riverpod.md`
- 下一步：按 `05-状态管理与Riverpod-闯关练习.md` 做闯关式练习；先从第 1 关局部 UI 状态开始，不急着写综合答案。
- 04 布局案例实验室已完成，可随时回看和追加自定义布局案例。

## 看板

| 状态 | 事项 | 备注 |
| --- | --- | --- |
| Todo | 02 Dart 基础与 TS 迁移 | 从语言差异开始 |
| Done | 03 Flutter 运行模型与 Widget | 已拆出生命周期 demo 和局部 State 示例 |
| Done | 04 布局样式和资源 | 已通过布局案例实验室练经典布局 |
| Doing | 05 状态管理与 Riverpod | 对齐目标项目 provider |
| Todo | 06 路由导航与页面组织 | 对齐 go_router |
| Todo | 07 异步网络数据模型 | 对齐 Dio 和 model |
| Todo | 08 本地存储权限与文件 | 对齐相册、权限、缓存 |
| Todo | 09 音视频与媒体能力 | 重点章节 |
| Todo | 10 工程化调试测试构建 | 建立门禁 |
| Todo | 11 flutter-spell-ai 接手导读 | 系统学习后进入 |

## Session 记录

### 2026-08-20 初始化

- 明确学习路线：先系统学习 Flutter/Dart，再接手 `flutter-spell-ai`。
- 明确额外重点：音视频内容。
- 创建 `AGENTS.md` 作为后续 agent 每次 session 的入口文件。
- 创建 `notes/` 学习章节骨架。

### 2026-08-21 Widget 与 State 拆分练习

- 先提交当前学习文档和示例代码保存点：`5d4a22f docs: 补充 Flutter 学习章节教学文档`。
- 将首页 `MyHomePage` 从 `StatefulWidget` 拆成无状态页面壳。
- 新增 `CounterSection` 局部管理计数器状态，演示 `setState` 只影响当前 State 对应的 build 范围。
- 验证 `fvm flutter analyze`、`fvm flutter test test/widget_test.dart`、`fvm flutter test` 全部通过。

### 2026-08-21 生命周期与布局实验室

- 为 `CounterSection` 添加 `initState()`、`didUpdateWidget()`、`dispose()` 调试打印，方便观察生命周期触发时机。
- 将首页改成 `Flutter 学习目录`，保留 `03 Widget 生命周期` 入口。
- 新增 `04 布局案例实验室`，包含居中、横向信息卡、比例分栏、列表、网格、叠层、固定头部滚动、响应式、安全区等经典布局案例。
- 下一步重点不是背 Widget，而是“点开效果 -> 找同名类 -> 改一个参数 -> 自己新增一个案例入口”。

### 2026-08-25 状态管理与 Riverpod 练习

- 确认 04 布局章节已学完，进入 05 状态管理。
- 为项目接入 `flutter_riverpod`，在 App 根部加入 `ProviderScope`。
- 新增 `lib/chapter_05/case_00_state_management_lab_page.dart`，用任务列表练习 `NotifierProvider`、`FutureProvider`、派生 `Provider`、`watch/read/listen`。
- 首页新增 `05 状态管理与 Riverpod` 入口。
- 将 05 章拆成 6 个独立案例：`setState`、`NotifierProvider`、`Provider`、`FutureProvider`、`AsyncNotifier + repository`、综合任务看板。

### 2026-08-26 状态管理闯关路线

- 明确当前卡点属于 B：provider 类型、`watch/read/listen`、异步状态等单点还会混。
- 新增 `notes/05-状态管理与Riverpod-闯关练习.md`，把综合案例拆成 6 个递进关卡。
- 下一步不直接写大综合案例，而是从“局部 UI 状态”开始，一关只练一个状态管理判断点。

## 阻塞与问题

- 暂无。

## 下次 Session 建议

从 `05-状态管理与Riverpod.md` 继续：

1. 阅读 `notes/05-状态管理与Riverpod-闯关练习.md`。
2. 新建第 1 关独立练习页面，先只用 `StatefulWidget + setState`。
3. 第 1 关完成后，再进入 `NotifierProvider` 关卡，不急着碰异步。
