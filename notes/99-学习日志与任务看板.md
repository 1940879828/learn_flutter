# 99 学习日志与任务看板

## 当前学习路线

系统学习 Flutter/Dart，然后再接手 `/Users/dev/Documents/Projects/flutter-spell-ai`。

## 当前阶段

- 阶段：继续补齐接手 SpellAI 前的专项能力章节。
- 当前章节：`19-34 Flutter 方法论手册`
- 下一步：用 18 手册回看 SpellAI 的入口、路由、状态、网络、媒体、权限和高风险模块；遇到具体任务时，按主题查 19-34 方法论。
- 12 章作为 SpellAI 接手路线图，不做首页 UI 入口；13/14/15 是可操作练习入口，16-32 是方法论与排查型参考手册。

## 看板

| 状态 | 事项 | 备注 |
| --- | --- | --- |
| Todo | 02 Dart 基础与 TS 迁移 | 从语言差异开始 |
| Done | 03 Flutter 运行模型与 Widget | 已拆出生命周期 demo 和局部 State 示例 |
| Done | 04 布局样式和资源 | 已通过布局案例实验室练经典布局 |
| Done | 05 状态管理与 Riverpod | 已有 Riverpod 分层案例和闯关路线 |
| Done | 06 转场动画实验 | 已接入 flutter_animate，练 PageRouteBuilder、Hero、错峰和常见转场组合 |
| Doing | 07 路由导航与页面组织 | 已恢复 go_router 实验，但不包含 Android 预测返回示例 |
| Done | 08 异步网络数据模型 | 已有模拟 API client、repository、model JSON、三态 UI |
| Done | 09 本地存储权限与文件 | 已有最近 prompt、temp JSON 文件、模拟权限 UI |
| Done | 10 音视频与媒体能力 | 已有媒体来源说明、图片预览、视频生命周期模拟 |
| Done | 11 工程化调试测试构建 | 已有工程化检查清单页 |
| Done | 12 flutter-spell-ai 接手导读 | 已补入口、路由、网络、model、媒体、风险边界路线图 |
| Done | 13 多语言与本地化 | 已有 locale、fallback、placeholder demo |
| Done | 14 Firebase 登录与账号状态 | 已有状态模拟和真实 SpellAI Firebase + 后端用户同步页签 |
| Done | 15 聊天键盘与列表性能 | 已有静态长聊天页和图片消息，用系统默认键盘复刻整页上移，并接入 native 逐帧 keyboard inset 驱动 |
| Done | 16 Flutter 性能症状排查手册 | 已按“已出现卡顿后排查修复优先，开发前预防其次”的方式沉淀 9 类坏味道 |
| Done | 17 引导页设计与制作方法论 | 已沉淀 onboarding 决策 brief、主流类型、Flutter 技术拆分和 AI/创作类 App 注意事项 |
| Done | 18 接手陌生 Flutter 项目方法论 | 已沉淀真实 Flutter 项目的读码顺序、风险分层、改动选型和验证闭环 |
| Done | 19 状态管理选型方法论 | 已沉淀状态生命周期、副作用边界和 Riverpod provider 类型选择 |
| Done | 20 路由与页面流设计方法论 | 已沉淀返回栈、参数恢复、登录回跳和 go_router API 选择 |
| Done | 21 异步请求与错误状态建模方法论 | 已沉淀 loading、empty、error、retry、cancel、stale、success 状态模型 |
| Done | 22 Repository 与数据模型分层方法论 | 已沉淀 ApiClient、Repository、Model、Provider/VM、Widget 职责边界 |
| Done | 23 本地存储选型方法论 | 已沉淀 prefs、SQLite、文件缓存、secure storage 和系统相册选型 |
| Done | 24 权限请求与降级体验方法论 | 已沉淀权限预热、拒绝降级、永久拒绝和 limited 状态处理 |
| Done | 25 媒体能力链路方法论 | 已沉淀来源、权限、预览、缓存、播放生命周期、保存和失败恢复 |
| Done | 26 AI 生成任务链路方法论 | 已沉淀 prompt、参数、额度、任务创建、轮询、结果页和重试保存 |
| Done | 27 登录与账号状态方法论 | 已沉淀 Firebase 用户、业务用户、token、auth state、失败回滚 |
| Done | 28 多语言与文案维护方法论 | 已沉淀 `.arb`、placeholder、description、gen-l10n 和布局检查 |
| Done | 29 加载态空状态错误态设计方法论 | 已沉淀非成功状态的用户反馈、恢复动作和验收标准 |
| Done | 30 动效与转场选择方法论 | 已沉淀 route transition、Hero、staggered、AnimatedSwitcher 和性能边界 |
| Done | 31 工程化验证与高风险变更方法论 | 已沉淀按风险选择 analyze、test、codegen、l10n、真机和回归验证 |
| Done | 32 聊天键盘与输入体验方法论 | 已沉淀键盘联动、输入栏、消息列表、滚动和复杂消息性能边界 |
| Done | 33 Flutter 生命周期方法论 | 已沉淀 StatefulWidget、父子组件、路由栈和 App 前后台生命周期读码方法 |
| Done | 34 Flutter 音视频处理方法论与开发指南 | 已沉淀音视频难点、SpellAI 播放器/缓存/保存相册链路和验证标准 |
| Done | 98 笔记模板 | 已补新增学习笔记、方法论手册和项目导读时的统一结构 |

## Session 记录

### 2026-08-20 初始化

- 明确学习路线：先系统学习 Flutter/Dart，再接手 `flutter-spell-ai`。
- 明确额外重点：音视频内容。
- 创建 `AGENTS.md` 作为后续 agent 每次 session 的入口文件。
- 创建 `notes/` 学习章节骨架。

### 2026-08-21 Widget 与 State 拆分练习

- 先提交当前学习文档和示例代码保存点：`5d4a22f docs: 补充 Flutter 学习章节教学文档`。
- 将首页 `MyHomePage` 从 `StatefulWidget` 拆成无状态页面壳。
- 新增 `CounterSection` 局部管理计数器状态，演示 `setState` 只影响当前 State 对应的 build 范围。
- 验证 `fvm flutter analyze`、`fvm flutter test test/widget_test.dart`、`fvm flutter test` 全部通过。

### 2026-08-21 生命周期与布局实验室

- 为 `CounterSection` 添加 `initState()`、`didUpdateWidget()`、`dispose()` 调试打印，方便观察生命周期触发时机。
- 将首页改成 `Flutter 学习目录`，保留 `03 Widget 生命周期` 入口。
- 新增 `04 布局案例实验室`，包含居中、横向信息卡、比例分栏、列表、网格、叠层、固定头部滚动、响应式、安全区等经典布局案例。
- 下一步重点不是背 Widget，而是“点开效果 -> 找同名类 -> 改一个参数 -> 自己新增一个案例入口”。

### 2026-08-25 状态管理与 Riverpod 练习

- 确认 04 布局章节已学完，进入 05 状态管理。
- 为项目接入 `flutter_riverpod`，在 App 根部加入 `ProviderScope`。
- 新增 `lib/chapter_05/case_00_state_management_lab_page.dart`，用任务列表练习 `NotifierProvider`、`FutureProvider`、派生 `Provider`、`watch/read/listen`。
- 首页新增 `05 状态管理与 Riverpod` 入口。
- 将 05 章拆成 6 个独立案例：`setState`、`NotifierProvider`、`Provider`、`FutureProvider`、`AsyncNotifier + repository`、综合任务看板。

### 2026-08-26 状态管理闯关路线

- 明确当前卡点属于 B：provider 类型、`watch/read/listen`、异步状态等单点还会混。
- 新增 `notes/05-状态管理与Riverpod-闯关练习.md`，把综合案例拆成 6 个递进关卡。
- 下一步不直接写大综合案例，而是从“局部 UI 状态”开始，一关只练一个状态管理判断点。

### 2026-08-27 调整第 6 章路由实验

- 调研 Flutter / Android predictive back 文档后，确认 Flutter 内置 predictive back builder 不支持“上一页固定全屏、当前页纯右滑收起”的预期效果。
- 预测返回示例从学习路线中移除，只保留普通路由导航练习。
- 由于 `06` 已用于转场动画实验，路由导航实验调整为第 `07` 章。

### 2026-08-27 转场动画实验室

- 新增 `06 转场动画实验室` 入口，用 `PageRouteBuilder` 演示 Slide、Fade、Scale、Rotation、Size、Shared Axis Z 和 Fade Through。
- 补充更偏产品体验的动效：Container Transform、Hero Header、Hero + Staggered、Collapsing Header、Parallax Header 和 AnimatedContainer。
- 该实验只练 Navigator 转场动画和页面内动效。

### 2026-08-27 恢复第 7 章路由实验

- 将原路由示例恢复到 `lib/chapter_07/`，首页入口为 `07 路由导航与页面组织`。
- 恢复 `go_router`、path、query、extra、`pop(result)` 和 redirect 示例。
- 不恢复 Android 预测返回按钮和页面。
- 登录拦截示例改为登录成功后 `pushReplacement(from)`，让详情页系统返回回到实验室列表，而不是退出 App。

### 2026-08-27 补齐 08-12 学习闭环

- 08 章新增 `lib/chapter_08/`：模拟 API client、repository、`SpellGenerationTask.fromJson/toJson`、loading/error/success UI，并在测试中覆盖 JSON roundtrip 和三态。
- 09 章新增 `lib/chapter_09/`：保存/读取最近 prompt、写入 temp JSON 文件、模拟相册权限状态；刻意不请求真实系统相册权限。
- 10 章新增 `lib/chapter_10/`：asset/network/file 媒体来源说明、图片预览 demo、`FakeVideoController` 生命周期模拟版；文档明确真实项目使用 `video_player`。
- 11 章新增 `lib/chapter_11/engineering_checklist_page.dart`：展示 pubspec、analyze、test、build_runner、gen-l10n 的判断清单。
- 首页 `LearningHomePage` 补充 08-11 入口；12 保持文档导读。
- 只读目标项目 `/Users/dev/Documents/Projects/flutter-spell-ai` 的 `AGENTS.md`、`pubspec.yaml`、入口、路由、远程 API、model、image2video、相册/图片/视频工具和自定义 lint，未修改目标项目。
- `notes/12-flutter-spell-ai接手导读.md` 已整理第一轮阅读顺序、模块地图、风险边界、可先改的小功能类型和暂时不要碰的模块。

### 2026-08-31 补充本地存储选型速记

- 在 09 章补充 SpellAI 本地存储分层：`Prefs`、`AppStorage`、`SQLite/sqflite` 和文件缓存，便于后续接手维护时快速判断状态、配置、列表数据和大文件分别放在哪里。

### 2026-08-27 新增多语言与 Firebase 登录章节

- 新增 `13-多语言与本地化` 文档和 `lib/chapter_13/` demo，用手写 catalog 练 locale、fallback、placeholder 和 key 心智。
- 新增 `14-Firebase登录与账号状态` 文档和 `lib/chapter_14/` demo，用 fake auth service 模拟 Google/Apple/Facebook/LINE、登录中、失败、取消、退出和 auth state。
- 首页补充 13/14 入口；12 接手导读仍保持文档入口，不做 UI。
- 只读 SpellAI 的 `lib/l10n/`、`firebase_service.dart`、`authentication.dart`、`login_page.dart`、`login_button.dart`，未修改目标项目。

### 2026-08-27 接入真实 SpellAI 登录实验

- 对照 `/Users/dev/Documents/Projects/learn-next` 的 Firebase Web 登录实现，确认可迁移的优化点是“后端 401/403 后强刷 Firebase ID token 并重试一次”，而不是 popup/Zustand/Server Action 这些 Web 专属入口。
- `lib/chapter_14/` 新增 `spellai_auth_config.dart`、`spellai_auth_models.dart`、`spellai_auth_service.dart`，真实页签支持 SpellAI Firebase 初始化、Google/Apple/Facebook/LINE 登录入口、`POST /users`、`GET /users/{uid}`、token retry 和退出登录。
- 真实配置从 `--dart-define` 读取，不写入源码；页面只展示 mask 后的 token 摘要、Firebase user 和完整 SpellAI user JSON。
- 文档已补真实运行方式、Web → App 映射、token 不落盘边界和剩余平台配置风险。

### 2026-08-28 使用 flutter_animate 优化第 6 章

- 通过 `fvm flutter pub add flutter_animate` 接入 `flutter_animate 4.5.2`，保留已有 `async` 依赖和本地 Java 17 配置。
- `lib/chapter_06/transition_lab_page.dart` 保留 `PageRouteBuilder` 教学主线，但把 route 转场效果改成 `child.animate(value: progress, autoPlay: false)` 绑定 Navigator 进度，再用 `.slideX()`、`.fadeIn()`、`.scale()`、`.rotate()` 表达视觉效果。
- Hero + Staggered 案例从手写 `StatefulWidget + AnimationController + Interval` 改成 `items.toList().animate(interval: 120.ms)`，减少样板代码。
- `AnimatedContainer` 仍保留隐式动画语义，外层增加一次轻微 `flutter_animate` scale 反馈，用来区分属性动画和装饰性反馈动画。
- 已验证 `fvm flutter analyze`、`fvm flutter test test/widget_test.dart --plain-name 'Transition lab opens every transition demo'` 和 `fvm flutter test` 通过。

### 2026-08-28 分析 macOS 启动警告

- 确认 `import 'package:go_router/go_router.dart';` 是 `lib/app.dart` 中 `GoRouter` 类型所需，不是启动警告根因。
- 启动日志主要来自 macOS CocoaPods / Xcode 原生构建：Firebase/Auth 第三方源码 warning、Xcode Metal toolchain search path warning，以及 Pods deployment target 过低。
- 在 `macos/Podfile` 的 `post_install` 中统一 Pods `MACOSX_DEPLOYMENT_TARGET = 10.15`，消除 10.11/10.12 deployment target 警告。
- 修复测试入口仍引用旧 `MyApp` 的 analyzer 错误，改为测试 helper 初始化 `App`、重置路由和登录态后 pump `AppScopeView`。
- 已验证 `fvm flutter analyze`、`fvm flutter test test/widget_test.dart`、`fvm flutter test`、`fvm flutter build macos --debug` 均通过；macOS build 仍保留 Xcode/Firebase 第三方 warning，但不阻塞启动。

### 2026-08-31 新增聊天键盘上移复刻

- 新增 `lib/chapter_15/chat_keyboard_jank_page.dart`，用静态长聊天消息、两张图片消息、底部输入栏和系统默认键盘复刻输入框聚焦后的整体上移动画。
- 首页新增 `15 聊天键盘上移动画` 入口，路由注册为 `/chapter-15/chat-keyboard-jank`。
- 新增 `notes/15-聊天键盘与列表性能.md`，记录 `MediaQuery.viewInsets`、整页 resize 和复杂消息列表性能风险。

### 2026-08-31 接入 native 逐帧键盘高度实验

- 通过 `flutter_keyboard_controller` 接入 Android `WindowInsetsAnimationCompat` / iOS keyboard frame 方向的逐帧键盘高度。
- `15 聊天键盘上移动画` 保持 `Scaffold.resizeToAvoidBottomInset: false`，改用 `KeyboardAvoidingView(position)` 驱动整块聊天内容上移，不再用 `MediaQuery.viewInsets` 作为动画源。
- 会话 header 已移出键盘位移层，当前效果是顶部 header 固定，消息列表和 prompt 输入栏跟随 native keyboard height 整体上移。
- 当时曾加入 `[chapter_15][prompt-height]` 日志观察 native keyboard height/progress；后续已按学习清理要求删除，当前以顶部状态条观察为主。

### 2026-08-31 新增 Flutter 性能症状排查手册

- 新增 `notes/16-Flutter性能症状排查手册.md`，定位为“已经出现卡顿后按症状排查修复”的参考文档，而不是泛泛性能 checklist。
- 手册先按 UI thread、Raster thread、平台交互、异步 IO、内存生命周期判断问题位置，再覆盖状态下沉、build 重活、懒加载、图片、动画、特效、platform view、异步加载、dispose 9 类坏味道。
- 每类坏味道都包含场景定义、常见写错方式、具体优化办法、适用边界和验收方式，方便后续 agent 按证据优化。

### 2026-09-01 新增引导页设计方法论

- 新增 `notes/17-引导页设计与制作方法论.md`，定位为以后写 onboarding 前先看的决策手册。
- 方法论先定义“第一次成功动作”，再判断是否需要完整前置引导，避免把引导页做成功能宣传轮播。
- 文档覆盖价值说明、个性化选择、权限预热、交互教学、渐进式引导、空状态引导、清单型引导，以及 Flutter 中的路由、状态、持久化、埋点和完成后跳转拆分。

### 2026-09-01 整理 notes 手册体系

- 将学习日志从 `notes/12-学习日志与任务看板.md` 重命名为 `notes/99-学习日志与任务看板.md`，避免与 `12-flutter-spell-ai接手导读.md` 编号冲突。
- 修正 README 和 AGENTS 中过期的 notes 路径，避免后续 session 按旧入口读错文档。
- 重构 `notes/01-索引.md`，拆成学习路线、项目接手、方法论手册、日志与看板四块。
- 新增 `notes/18-接手陌生Flutter项目方法论.md`，用于接手真实 Flutter 项目前建立入口、路由、状态、网络、存储、权限、媒体和高风险模块地图。
- 新增 `notes/98-笔记模板.md`，以后新增学习章节、方法论手册或项目导读时优先套用。

### 2026-09-01 补全 Flutter 方法论手册

- 新增 `notes/19-状态管理选型方法论.md` 到 `notes/32-聊天键盘与输入体验方法论.md`，覆盖状态、路由、异步、数据分层、存储、权限、媒体、AI 生成、登录、多语言、页面状态、动效、验证和聊天输入体验。
- 每篇方法论都按 `notes/98-笔记模板.md` 的结构写入：使用场景、一句话原则、决策表、固定检查问题、Flutter 实现拆分、常见坏味道、验收标准和 SpellAI 映射。
- `notes/01-索引.md` 的方法论手册区已补齐 19-32，后续可以按任务主题快速检索。

### 2026-09-01 阅读聊天键盘大文件

- 开始回看 `lib/chapter_15/chat_keyboard_jank_page.dart`，目标不是逐行硬读，而是先分出页面状态、生命周期监听、键盘事件、build 骨架、假数据和私有 Widget。
- 本次读码重点：理解为什么 622 行文件并不等于 622 行核心逻辑，先抓“键盘高度驱动哪一层移动”这条主线。

### 2026-09-01 新增 Flutter 生命周期方法论

- 新增 `notes/33-Flutter生命周期方法论.md`，系统整理 `StatefulWidget` 创建、依赖变化、父参数变化、构建、布局后回调、销毁、父子组件顺序、路由栈和 App 前后台生命周期。
- 用第 15 章聊天键盘文件套读：`initState` 绑定焦点监听，`build` 组合 `KeyboardAvoidingView` 和状态条，`dispose` 释放 `FocusNode` 与 `TextEditingController`。

### 2026-09-01 新增音视频处理指南

- 新增 `notes/34-Flutter音视频处理方法论与开发指南.md`，把音视频开发难点整理成表格，覆盖初始化首帧、生命周期、controller 释放、列表视频、下载缓存、保存相册、平台兼容、音频焦点和验证路径。
- 只读 SpellAI 目标项目，汇总其真实处理思路：`VideoPlayerControllers` 统一来源、`VideoPlayerWidget` 管路由和 App 前后台、`VideoView` 管列表可见性和下载、`VideoPlayerPool` 做播放器限流、`FileDownloadManager` 做缓存取消、`album_utils.dart` 做相册保存和权限。

### 2026-09-01 删除聊天键盘调试日志

- 按要求删除 `lib/chapter_15/chat_keyboard_jank_page.dart` 中输入栏高度调试相关代码，包括 `GlobalKey` 测量、文本变化监听、键盘帧 debug listener、`addPostFrameCallback` 和 `debugPrint`。
- 更新第 15 章和第 33 章笔记，改为通过顶部状态条观察 native keyboard height/progress，不再描述 `[chapter_15][prompt-height]` 日志。

### 2026-09-02 创建本地键盘 inset 插件

- 新增 `plugins/keyboard_insets_adapter` 本地 Flutter plugin，用薄 Dart API 暴露 `KeyboardInsetsProvider`、`KeyboardInsetsScope`、`KeyboardInsetsController`、`KeyboardInsetsState` 和 height/progress/visible notifiers。
- 移除 app 对 `flutter_keyboard_controller` 的直接依赖，改为 path dependency；插件保留 MIT license notice，不搬高级键盘 Widget。
- 第 15 章页面改为通过 `ValueListenableBuilder.child` 稳定聊天子树，并用页面内 `Transform.translate` 根据 native keyboard height 整体上浮消息列表和输入栏。
- 已验证 `fvm flutter pub get`、`fvm flutter analyze`、第 15 章 focused widget test、`fvm flutter test`、`fvm flutter build apk --debug`、`fvm flutter build apk --profile`、`fvm flutter build ios --debug --no-codesign`；Android 插件 Gradle 已做 AGP 8/9 条件兼容，当前 AGP 8.9.1 本机可构建，AGP 9 路径仍不 apply Kotlin plugin。

### 2026-09-04 设计 Chapter 16 视频本地文件缓存

- 新增 `docs/cache_video/README.md`，将视频缓存方案定位为稳定设计指南，而不是依赖当前代码行号的实施手册。
- 第一阶段选择完整文件缓存，通过 `VideoControllerFactory` 隔离播放器与具体缓存库，同时保持 `VideoPlayerSection` 对 controller 生命周期的唯一所有权。
- 文档明确区分播放缓冲、本地缓存、预加载和离线下载，并记录缓存身份、过期淘汰、异步退出、损坏恢复和后续分片缓存的升级条件。
- 下一步实施前需重新读取 `lib/chapter_16/player/` 当前代码，先建立不改变行为的播放来源接缝，再动态决定具体缓存库 API 和修改步骤。

### 2026-09-04 开始落地 Chapter 16 播放来源接缝

- 按 `docs/cache_video/README.md` 的第一阶段路线，从“先不改变播放行为”的来源接缝开始，而不是直接引入缓存库。
- 新增 `lib/chapter_16/player/source/video_controller_factory.dart`，用 `VideoControllerFactory` 表达“根据视频 URL 异步创建一个尚未初始化的 controller”。
- `VideoPlayerSection` 已开始接收 `controllerFactory`，并把 controller 创建迁移到 `_prepareController()` 的异步流程中。
- 本轮学习重点是 Dart nullable、`mounted` 检查、异步返回后页面可能已销毁，以及 controller 只能由 `VideoPlayerSection` 最终持有和释放。

## 阻塞与问题

- 暂无。

## 下次 Session 建议

用 18 手册回看 SpellAI，再把读码结果补进 12 接手导读；遇到具体任务时按主题查 19-34：

1. 对照 `notes/18-接手陌生Flutter项目方法论.md`，画出 SpellAI 的入口初始化、路由、状态注入、网络 API、存储、权限、媒体、支付/广告/Firebase 边界。
2. 如果是页面状态或请求问题，先查 `notes/19-状态管理选型方法论.md`、`notes/21-异步请求与错误状态建模方法论.md`、`notes/29-加载态空状态错误态设计方法论.md`。
3. 如果是生成、媒体、音视频、权限、登录或多语言问题，先查 `notes/24-权限请求与降级体验方法论.md`、`notes/25-媒体能力链路方法论.md`、`notes/26-AI生成任务链路方法论.md`、`notes/27-登录与账号状态方法论.md`、`notes/28-多语言与文案维护方法论.md`、`notes/34-Flutter音视频处理方法论与开发指南.md`。
4. 如果是路由、动效、键盘、生命周期或验证问题，先查 `notes/20-路由与页面流设计方法论.md`、`notes/30-动效与转场选择方法论.md`、`notes/32-聊天键盘与输入体验方法论.md`、`notes/33-Flutter生命周期方法论.md`、`notes/31-工程化验证与高风险变更方法论.md`。

## 剩余风险

- 本次未修改目标项目，也未验证 SpellAI 在本机能否完整运行。
- 10 章是视频生命周期模拟版，未引入真实 `video_player` 播放器；进入真实媒体改动前仍需模拟器/真机验证。
- 09 章权限为模拟状态，未触发 `permission_handler` 或系统相册权限弹窗。
- 13 章未接真实 `gen-l10n`，只是教学模拟；真实项目修改多语言必须改 `.arb` 并跑 `fvm flutter gen-l10n`。
- 14 章已接真实 Firebase SDK 和 SpellAI 后端契约，但默认不带真实配置；需要本机 `--dart-define`、平台 OAuth 配置、bundle id / SHA / URL scheme 才能在 App 端完整验证。
- 14 章真实页签会向 SpellAI 后端测试/正式环境创建或同步用户；运行前必须确认使用的环境和测试账号。
