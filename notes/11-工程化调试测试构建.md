# 11 工程化、调试、测试与构建

## 学习目标

- 掌握 Flutter 项目的日常命令、调试方式、测试层级和构建心智。
- 理解 FVM、pubspec、代码生成、lint、多端工程目录。
- 为安全修改目标项目建立基本门禁。

## Web 前端迁移映射

- `package.json` 对应 `pubspec.yaml`。
- `npm install` / `pnpm install` 对应 `flutter pub get`。
- `eslint` / `tsc` 对应 `flutter analyze` 和 Dart analyzer。
- Next build 对应 Flutter 不同平台 build，但移动端还涉及签名、权限、原生配置。
- codegen 类似 GraphQL/Prisma/OpenAPI 生成，但 Flutter 常用 `build_runner`。

## 核心概念清单

- FVM
- `pubspec.yaml`
- `pubspec.lock`
- `analysis_options.yaml`
- `flutter analyze`
- `flutter test`
- widget test
- integration test
- `build_runner`
- iOS/Android 平台目录
- 日志、断点、DevTools

## 最小练习

- `lib/chapter_11/engineering_checklist_page.dart`：一页工程化检查清单。
- `test/widget_test.dart`：验证首页新增入口和 08 章关键行为。

操作顺序：

1. 从首页进入 `11 工程化调试测试构建`。
2. 逐项解释 `pubspec.yaml`、`analysis_options.yaml`、widget/unit test、`build_runner`、`gen-l10n` 的触发条件。
3. 修改代码后跑 `fvm flutter analyze` 和 `fvm flutter test`。
4. 只有改了依赖、assets、代码生成或多语言时，才额外跑对应命令。

## 目标项目观察

优先阅读：

- `/Users/dev/Documents/Projects/flutter-spell-ai/pubspec.yaml`
- `/Users/dev/Documents/Projects/flutter-spell-ai/analysis_options.yaml`
- `/Users/dev/Documents/Projects/flutter-spell-ai/plugins/spellai_lint/`
- `/Users/dev/Documents/Projects/flutter-spell-ai/test/`

观察问题：

- 目标项目有哪些私有 hosted package 和 git package？
- 哪些文件由 build_runner 生成？
- 自定义 lint 约束了什么？

本章和 SpellAI 的直接映射：

- SpellAI 的 `pubspec.yaml` 明确 Flutter `3.35.7`、Dart `>=3.9.0 <4.0.0`，依赖包括 `dio`、`go_router`、`flutter_riverpod`、`image_picker`、`photo_manager`、`video_player`、`sqflite`、Firebase、内购、广告和私有 hosted/git package。
- `analysis_options.yaml` 排除了 `plugins/` 和 `**/*.g.dart`，并把 `always_declare_return_types`、`avoid_void_async`、`only_throw_errors`、`prefer_relative_imports` 等规则提升为 error。
- `plugins/spellai_lint/` 里自定义了 `always_declare_formal_parameter_types`，要求函数/方法参数显式声明类型。
- `build_runner` 主要服务 `json_serializable` 等生成链路，`*.g.dart` 通常不手改。
- `flutter gen-l10n` 服务 `.arb` 多语言生成，SpellAI 的 `flutter.generate: true` 和 `lib/l10n/` 需要一起看。

## 完成标准

- 能解释 pubspec 中 dependencies 和 dev_dependencies 的区别。
- 能知道什么时候需要跑 build_runner。
- 能在修改前后选择合适的 focused test 和工程门禁。
- 能读懂 SpellAI 的 analyzer 错误级别，避免把本学习项目的宽松规则误套到目标项目。
- 能说明哪些改动需要更新 `pubspec.lock`，哪些不需要。

## QA 问答

### Q1：`fvm` 为什么值得在 Flutter 学习阶段使用？
它固定团队和项目的 Flutter 版本，避免不同机器 Flutter 差异导致依赖解析、代码生成和编译行为不一致。

### Q2：`pubspec` 变更和 `pubspec.lock` 有什么关系？
`pubspec` 声明范围，`lock` 固定实际解析版本，团队协作时 lock 有助于复现实验环境和 CI 一致性。

### Q3：`flutter analyze` 和 `flutter test` 分别解决什么问题？
`analyze` 关注静态问题和规范，`test` 关注运行时行为与契约验证，两者都是修改前后的必要门禁。

### Q4：为什么会有 `build_runner` 命令？
为代码生成（如 json 序列化、本地化）生成一致、可追踪的产物，避免手写映射导致的人为错误。

### Q5：提交代码前你会跑什么最小门禁？
至少是 `analyze`、关键路径 `test`、以及变更相关的手工验收；复杂改动再补充集成/设备级验证。
